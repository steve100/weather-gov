[project]
name = "weather-gov"
version = "0.1.0"
description = "Fetch current weather and forecasts from the weather.gov API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "html2text>=2025.4.15",
    "playwright>=1.50.0",
]

